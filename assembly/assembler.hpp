/**
 * assembler.hpp
 * A class that implements a de Bruijn graph-based assembler
 * to assemble sequence from their constitutive kmers.
 * Uses a colored dBG much like Cortex / Platypus to allow
 * multiple paths at locations in the graph.
 * Eric T Dawson
 */
